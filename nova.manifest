# nova.manifest — app metadata for the nova updater (this file *is* the convention)
NAME=nova-updater
DESCRIPTION=Git-based installer/updater for Nova Network tools (CLI + GTK3 GUI)
VERSION=0.1.0
TYPE=cli
SCOPE=user
INSTALLER=install.sh
